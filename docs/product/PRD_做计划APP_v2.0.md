# 做计划 APP 产品需求文档（PRD v2.0）

## 📌 文档信息

| 项目 | 内容 |
|------|------|
| **产品名称** | 做计划 APP |
| **文档版本** | v2.0（落地版/可直接研发） |
| **编写日期** | 2026-01-27 |
| **依据材料** | 《做计划APP.pdf》（原始需求）<br>`plan-app-prd-supplement.md`（歧义修复与状态机补全） |
| **约束说明** | 不新增模块/不推翻设计，仅对 4 模块（做计划/视图/专注/我的）补齐规则、边界、异常与状态机 |

---

## 📚 目录

1. [产品概述](#1-产品概述)
2. [全局口径（必须一致）](#2-全局口径必须一致)
3. [模块需求](#3-模块需求)
   - 3.1 [做计划模块](#31-做计划模块)
   - 3.2 [视图模块](#32-视图模块)
   - 3.3 [专注模块](#33-专注模块)
   - 3.4 [我的模块](#34-我的模块)
4. [异常与边界（落地必须覆盖）](#4-异常与边界落地必须覆盖)
5. [附录](#5-附录)
   - 5.1 [术语表](#51-术语表)
   - 5.2 [关键状态机](#52-关键状态机)

---

## 1. 产品概述

### 1.1 定位

这是一个偏「个人任务 / GTD + 四象限」的计划管理 APP，核心特点：
- 以"天"为主视角
- 任务用 P0 至 P3 四象限组织
- 强调长期坚持（打卡、连续天数）
- 弱项目、强执行

### 1.2 模块结构

总共 4 个模块（底部导航 Tab）：
- **Plan（做计划）**：创建/管理任务、四象限视图、日历选择 ✅ 已实现
- **View（视图）**：周/月视角监控待执行/逾期/已完成 ✅ 已实现
- **Focus（专注）**：倒计时番茄钟、专注历史统计 ✅ 已实现
- **Me（我的）**：头像管理、昵称管理、音效设置、语言设置、设备管理、修改密码、退出登录 ✅ 已实现

**全局 UI 语言**：多语言支持（英文/中文/日文）

### 1.3 多语言支持（已实现）

应用支持三种语言，用户可在 Me → Settings → Language 中切换：

| 语言 | 代码 | 应用标题 | 说明 |
|------|------|----------|------|
| **English** | `en` | Maiden Plan | 默认语言 |
| **中文** | `zh` | 少女计划 | 简体中文 |
| **日本語** | `ja` | 少女の計画 | 日文 |

**技术实现**：
- 服务：`locale_service.dart`（单例模式）
- 全局翻译函数：`tr(key)` 用于获取当前语言的翻译文案
- 语言持久化：使用 `SharedPreferences` 保存用户选择
- 实时切换：选择语言后立即生效，无需重启应用
- 所有页面、浮层、弹窗均已国际化

---

## 2. 全局口径（必须一致）

### 2.1 核心对象定义

| 对象 | 定义 | 关键属性 |
|------|------|----------|
| **任务（Todo）** | 归属某一"日期（归属日期）"与某一象限优先级（P0~P3），支持完成/未完成、可包含子任务、可设置重复 | `title`（标题）<br>`date`（归属日期）<br>`priority`（P0~P3）<br>`status`（未完成/已完成）<br>`createdAt`（创建时间）<br>`completedAt`（完成时间）<br>`deletedAt`（删除时间/逻辑删除）<br>`repeatType`（重复类型：不重复/每日/每周/每月）<br>`repeatConfig`（重复配置：如每周一三五/每月1号）<br>`isRepeatInstance`（是否为重复任务副本）<br>`repeatParentId`（重复任务源 ID） |
| **子任务（SubTodo）** | 必须挂载在某个父任务下，支持完成/未完成、可设置重复 | `title`（标题）<br>`parentId`（父任务 ID）<br>`status`（未完成/已完成）<br>`repeatType`（重复类型：不重复/每日/每周/每月）<br>`repeatConfig`（重复配置）<br>`isRepeatInstance`（是否为重复子任务副本）<br>`repeatParentId`（重复子任务源 ID） |
| **打卡（Check-in）** | **存在明确"打卡/完成今日计划"按钮，点击才算打卡**（影响坚持天数、7 天未打卡注销、未打卡推送） | `date`（打卡日期）<br>`checkedAt`（打卡时间）<br>`consecutiveDays`（连续天数） |
| **视图聚合（View Aggregation）** | 对任务按周/月范围、按状态（待执行/逾期/已完成）聚合展示 | - |
| **专注会话（Focus Session）** | 倒计时番茄钟会话，按规则计入总专注时间 | `duration`（时长/分钟）<br>`type`（类型/固定枚举）<br>`elapsedSeconds`（已完成秒数）<br>`counted`（是否计入总时长） |
| **会员（Entitlement）** | **免费期**：非会员免费使用 1 个月，从**首次微信授权登录/注册**起算<br>**会员有效期**：购买成功后得到到期时间 | `trialStartAt`（免费期起算时间）<br>`expireAt`（会员到期时间）<br>`status`（未激活/免费期/会员有效/已到期） |
| **账号状态** | 未登录/已登录/已注销（Deactivated） | `deactivatedAt`（注销时间）<br>`lastCheckInDate`（最近打卡日期）<br>`consecutiveUncheckDays`（连续未打卡天数） |

### 2.2 任务状态与父子联动（强制规则）

- **父任务存在子任务时**：父任务完成状态由子任务派生：
  - 全部子任务完成 ⇒ 父任务完成
  - 任一子任务未完成 ⇒ 父任务未完成
  - 父任务完成后新增未完成子任务 ⇒ 父任务自动变为未完成
- **父任务不提供"绕过子任务的强制完成"**（避免口径分裂）

### 2.3 "完成态过滤"开关口径

- **完成态过滤开关含义**：
  - **开**：显示已完成任务（以删除线样式展示）
  - **关**：隐藏已完成任务
- **默认值**：开（显示删除线）
- **影响范围**：仅影响展示，不影响任务状态与统计

### 2.4 日历彩色小圆点口径

- 小圆点只反映【所选日期】归属的任务中，各象限是否存在**未完成**任务：
  - 例如所选日期存在未完成 P0 ⇒ 显示红色小点
- **逾期任务不并入所选日期小圆点**（逾期仅在视图模块展示）

### 2.5 逾期天数 N 的计算口径

- 以用户本地时区为准，按自然日计算
- 当 `today(00:00)` 超过任务归属日期的 `date(00:00)` 时视为逾期
- \(N = todayDate - taskDate\)，单位：天，整数

### 2.6 视图默认态与记忆

- **首次进入视图页默认**：周视图 + 待执行
- **用户切换后记忆上次选择**：下次进入保持上次周/月与状态 Tab
- 若无历史选择（首次安装/清缓存）：按默认进入

### 2.7 专注会话计入规则

- 倒计时自然结束 ⇒ 计入总专注时间
- 中途手动结束：
  - `< 50%`：不计入
  - `≥ 50%`（含等于）：计入已完成时长
- 计算使用秒：`elapsedSeconds / durationSeconds`；计入时长 = `elapsedSeconds`（自然结束则 = `durationSeconds`）
- **展示按小时向下取整**（例：3700s 展示 1h；小于 1 小时展示 0h）

### 2.8 多设备退出登录口径

- 设备列表中的【退出登录】：仅对所选"非当前设备"生效（吊销该设备 token）
- 页面底部【退出登录】主按钮：仅退出当前设备（吊销当前 token），不影响其他设备

### 2.9 7 天未打卡注销 + 推送

- **连续未打卡判定**：按用户本地时区自然日统计
  - 每天 00:00 结算昨日是否打卡
  - 昨日未打卡：连续未打卡计数 +1；一旦某日打卡则清零
- **连续未打卡达到 7 天**：账号置为 **Deactivated**（逻辑禁用），禁止再次登录与使用
- **未打卡提醒推送**：
  - 对"未注销且当日未打卡"的账号，每日仅推送 1 次
  - 默认发送窗口：20:00–21:00（可配置）
  - 账号进入 Deactivated 后停止所有未打卡提醒推送
- **注销后数据处理**：**仅做物理删除**（触发注销后执行），不支持恢复

---

## 3. 模块需求

## 3.1 做计划模块

### 3.1.1 页面结构

| 区域 | 内容 | 说明 |
|------|------|------|
| 左上 | 用户信息（头像、昵称） | 点击进入个人中心 |
| 左上 | 坚持天数统计：「已坚持做计划 N 天」 | 连续打卡天数，见 2.9 |
| 右上 | 完成态过滤开关 | 见 2.3 |
| 顶部 | 年/月/日 + 一周横向日历 | 可点击日期；当前选中高亮 |
| 顶部右侧 | **"返回今天"按钮** | **仅当选中非今日日期时显示**<br>点击后：日历自动选中今天，且任务列表切换到今天 |
| 日期下方 | 彩色小圆点 | 见 2.4 |
| 中间 | 四象限任务列表（P0/P1/P2/P3） | 见 3.1.2 |
| 底部 | 大号 "+" 浮动按钮 | 新增任务，见 3.1.4 |
| 底部/顶部 | "打卡/完成今日计划"按钮 | 位置由 UI 决定，见 3.1.6 |

### 3.1.2 四象限定义（艾森豪威尔矩阵）

| 象限 | 标题 | 含义 | 颜色（已实现） |
|------|------|------|----------|
| **P0** | DO FIRST / 立即执行 / 最優先 | 紧急且重要 - 现在就做 | 玫红色 `#E91E63` |
| **P1** | SCHEDULE / 计划安排 / スケジュール | 重要不紧急 - 安排时间做 | 淡粉红 `#F48FB1` |
| **P2** | DELEGATE / 尽快处理 / 委任 | 紧急不重要 - 尽快处理 | 低饱和橙 `#E8A87C` |
| **P3** | ELIMINATE / 延后或放弃 / 削減 | 不重要不紧急 - 可以不做 | 低饱和灰绿 `#90A4AE` |

**象限标题（已国际化）**：
- 象限区域顶部显示 "EISENHOWER MATRIX" 标题（支持多语言）
- 每个象限显示对应标题和描述，根据当前语言动态切换

**象限区域交互（已实现）**：
- 每个象限区域固定高度（160px），任务超出时支持**上下滚动**
- 点击象限**空白区域**可快速创建该优先级的任务（自动预选象限）

### 3.1.3 任务展示与交互

#### 任务卡片组成
- 任务名称（单行显示，超长省略）
- 左侧圆形勾选按钮：点击切换完成/未完成
- 已完成展示删除线（若完成态过滤开=显示）
- **子任务图标**（已实现）：若任务包含子任务，右侧显示子任务图标及完成进度（如"2/3"），点击可展开子任务列表浮层

#### 任务完成交互（已实现）

**点击圆形勾选按钮**：
- 状态切换带**200ms 动画过渡**：
  - 未完成 → 已完成：圆圈填充颜色 + 显示勾选图标 + 文字变灰色删除线
  - 已完成 → 未完成：圆圈恢复空心 + 隐藏勾选图标 + 文字恢复正常
- 若父任务有子任务：点击父任务圆圈会**同时切换所有子任务**的完成状态

#### 任务删除交互（已实现）

**左滑删除**：
- 在任务卡片上**向左滑动**，右侧露出删除按钮
- 删除按钮样式：24x24px 圆角按钮，颜色与当前象限优先级一致
- 删除按钮**渐显动效**：随滑动距离逐步显示（透明度 + 位移 + 缩放）
- 最大滑动距离：32px
- 松手未触发删除时自动回弹
- 点击删除按钮：立即从当前象限移除任务（前端交互，不涉及后端）

#### 排序规则
- 同一象限内排序：**创建时间升序**（早创建的在前）

#### 子任务展示（已实现）

**子任务列表浮层**：
- 点击父任务右侧的子任务图标，从底部弹出子任务列表浮层
- 浮层标题："Subtasks" + 优先级徽章（如 P0）
- 显示父任务信息卡片（任务名称 + 完成进度如"2/3"）
- 子任务列表固定高度（280px），超出时支持滚动
- 子任务按**创建时间倒序**排列（最新的在前）
- 统一使用 P0 红色主题，仅优先级徽章保留实际颜色

**子任务项交互**：
- 左侧圆形勾选按钮：点击切换完成/未完成（带动画）
- 点击子任务名称或右侧编辑图标（✎）：进入**行内编辑模式**，直接修改名称
- 左滑删除：与父任务删除交互一致
- 父子联动按 2.2 执行（所有子任务完成 → 父任务自动完成）

#### 字段约束
| 字段 | 约束 |
|------|------|
| 任务标题 | 必填，1-100 字符，支持 emoji |
| 子任务标题 | 必填，1-50 字符，支持 emoji |
| 归属日期 | 必填，不可早于"今天-365天"（历史 1 年）；不可晚于"今天+365天"（未来 1 年） |
| 子任务数量 | 单个任务下最多 20 个子任务 |

### 3.1.4 新增与编辑

#### 新增任务（浮层交互）

**触发入口**：底部"+"浮动按钮

**交互流程**：
1. 点击"+"按钮 → 从底部弹出浮层（半屏/全屏由 UI 决定，建议半屏）
2. 浮层自动获取焦点到"任务标题"输入框

**浮层内容**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| **任务标题** | 文本输入框 | 必填，1-100 字符，支持 emoji<br>placeholder："输入任务名称" | 空 |
| **象限选择** | 单选按钮组（4 个选项） | P0 / P1 / P2 / P3<br>每个选项展示颜色标识与含义<br>（如：P0 红色"紧急且重要"） | P1 |
| **重复设置** | 可选开关 + 选择器 | 见下方"重复设置规则" | 不重复 |
| **底部按钮** | 操作区 | "取消" / "保存" | - |

**交互细节**：
- **归属日期**：任务自动归属日历当前选中的日期，**浮层中不显示日期字段**
  - **若选中的是历史日期**（< today），则自动改为 today，并在浮层顶部显示提示："Auto-adjusted to today"
  - 用户若需修改归属日期，需先关闭浮层 → 点击日历切换日期 → 重新点击"+"

**视觉样式（已实现）**：
- **标题**："New Task"，使用 P0 红色
- **关闭按钮**：右上角 X 按钮（替代原"取消"按钮）
- **输入框**：仅保留底部下划线（P0→P1 渐变），无边框
- **象限选择**：使用 P0/P1/P2/P3 简洁标签
- **重复设置开关**：使用 P1 粉色
- **月重复选择**：点击弹出日历网格弹窗（1-31日选择）
- **保存按钮**："Save Task →"，P0→P1 渐变胶囊按钮
- **象限选择**：
  - 4 个单选按钮横向排列（或 2x2 宫格）
  - 每个选项显示：象限标签（如 P0）+ 颜色圆点 + 含义文字（如"紧急且重要"）
  - 默认选中 P1
- **重复设置**（见下方详细规则）：
  - 默认关闭（不重复）
  - 打开开关后，展开重复选项（每日/每周/每月）
- **保存逻辑**：
  - 点击"Save Task" → 校验任务标题（不为空 且 长度 1-100）
  - 校验通过 → 创建任务（若设置了重复，自动生成重复任务）→ 关闭浮层 → 任务出现在对应象限区域
  - **校验失败交互（已实现）**：
    - 输入框触发轻微**震动动效**（shake animation，400ms）
    - 输入框下划线变为**红色**
    - 下方淡入显示错误提示："Please enter a valid task name"
    - 3 秒后自动消失，用户输入时立即清除
- **取消逻辑**：
  - 点击右上角 X 按钮 / 点击浮层外区域（蒙层） / 返回键 → 关闭浮层，不保存

**重复设置规则**：

| 重复类型 | 配置项 | 生成规则 | 示例 |
|----------|--------|----------|------|
| **不重复** | - | 仅创建当日任务 | - |
| **每日重复** | 无需额外配置 | 从当前归属日期起，**每天**自动生成一个任务副本（未来 365 天） | 设置"早起跑步"每日重复 → 明天/后天…每天都自动生成一个"早起跑步"任务 |
| **每周重复** | 选择星期几<br>（多选，如周一/周三/周五） | 从当前归属日期起，**每周**在指定星期自动生成任务副本（未来 52 周） | 设置"团队会议"每周一/周三重复 → 下周一/下周三…每周一三都生成任务 |
| **每月重复** | 选择几号<br>（单选，如每月 1 号） | 从当前归属日期起，**每月**在指定日期自动生成任务副本（未来 12 个月） | 设置"还信用卡"每月 1 号重复 → 下个月 1 号/下下个月 1 号…都生成任务 |

**重复任务特性**：
- **自动生成**：创建重复任务后，系统自动在未来日期生成任务副本（不占用当前任务数上限）
- **独立管理**：每个副本都是独立任务，可单独完成/编辑/删除
- **标识**：重复任务在任务卡片右上角显示"🔁"标识
- **停止重复**：
  - 方式 1：编辑任务 → 关闭"重复设置"开关 → 保存（仅影响未来副本，已生成的副本不受影响）
  - 方式 2：删除任务 → 二次确认"是否删除所有重复任务？"（包含已生成的所有副本）
- **修改重复规则**：编辑任务 → 修改重复类型/配置 → 保存（仅影响未来副本，已生成的副本不受影响）

**字段约束**：
- 重复任务的**归属日期范围**：从当前日期起，最多生成未来 365 天的副本
- 重复任务**总数上限**：单个重复任务最多生成 365 个副本（防止数据量过大）

**示例**：
- 用户在日历上选中"01-28（明天）" → 点击"+" → 任务自动归属 01-28
- 用户在日历上选中"01-25（历史）" → 点击"+" → 任务自动归属今天 + 顶部提示"已自动调整为今天"
- 用户创建"早起跑步"任务 → 打开"重复设置" → 选择"每日重复" → 保存 → 系统自动在未来 365 天每天生成一个"早起跑步"任务

---

#### 新增子任务（浮层交互）

**触发入口（已实现）**：
1. **点击父任务卡片**：直接弹出新增子任务浮层
2. 或点击父任务右侧子任务图标 → 打开子任务列表浮层 → 可在浮层内操作

**交互流程**：
1. 点击父任务卡片 → 从底部弹出"New Subtask"浮层
2. 浮层自动获取焦点到"子任务标题"输入框

**浮层内容（已实现）**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| **标题** | 只读文本 | "New Subtask"（P0 红色） | - |
| **父任务名称** | 可编辑文本 | 显示父任务标题，**点击可直接编辑**父任务名称 | 自动关联 |
| **子任务标题** | 文本输入框 | 必填，1-100 字符<br>placeholder："What needs to be done?" | 空 |
| **关闭按钮** | 右上角 X | 关闭浮层 | - |
| **保存按钮** | 渐变按钮 | "Save Subtask →"（P0→P1 渐变） | - |

**视觉样式（已实现）**：
- 与"New Task"浮层保持一致的 UI 语言
- 标题使用 P0 红色
- 输入框仅保留底部下划线（P0→P1 渐变）
- 保存按钮使用 P0→P1 渐变胶囊样式
- 无重复设置、无象限选择（简化版）

**父任务名称编辑（已实现）**：
- 父任务信息区域显示优先级圆点 + 任务名称 + 编辑图标（✎）
- 点击可进入编辑模式，直接修改父任务名称
- 失焦或按回车保存
- 空值保护：若输入为空则恢复原标题

**交互细节**：
- **父任务关联**：子任务自动挂载在当前父任务下，不可修改
- **归属日期**：子任务继承父任务的归属日期，不可单独设置
- **象限**：子任务继承父任务的象限，不可单独设置
- **保存逻辑**：
  - 点击"Save Subtask" → 校验子任务标题（不为空 且 长度 1-100）
  - 校验通过 → 创建子任务 → 关闭浮层 → 子任务出现在父任务子任务列表中
  - **校验失败交互**：与新增任务一致（震动 + 红色下划线 + 错误提示）
- **数量限制**：
  - 若父任务下已有 20 个子任务 → 点击时 toast 提示"子任务数已达上限（20 条）"，不弹出浮层

---

#### 编辑任务（浮层交互）

**触发入口**：
1. 点击任务卡片 → 进入"任务详情页" → 点击"编辑"按钮
2. 或在任务卡片上**右滑**露出"编辑"按钮

**交互流程**：
1. 点击"编辑" → 从底部弹出浮层（与新增任务浮层一致）
2. 浮层自动填充当前任务信息

**浮层内容**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| **任务标题** | 文本输入框 | 必填，1-100 字符 | 当前任务标题 |
| **归属日期** | 日期选择器（可编辑） | 点击展开日期选择器<br>受字段约束限制（历史 1 年/未来 1 年） | 当前归属日期 |
| **象限选择** | 单选按钮组 | P0 / P1 / P2 / P3 | 当前象限 |
| **重复设置** | 可选开关 + 选择器 | 见"重复设置规则" | 当前重复设置 |
| **底部按钮** | 操作区 | "取消" / "保存" / "删除"（红色） | - |

**交互细节**：
- **归属日期可编辑**：点击日期字段 → 展开日期选择器（日历组件）→ 选择日期 → 自动关闭选择器
  - **支持跨日修改**，但受字段约束限制（历史 1 年/未来 1 年）
- **删除按钮**：点击"删除" → 二次确认弹窗"确认删除该任务？" → 确认 → 删除任务 → 关闭浮层
- **保存逻辑**：
  - 点击"保存" → 校验字段 → 更新任务 → 关闭浮层
  - 若修改了归属日期 → 任务移动到新日期的对应象限

---

#### 编辑子任务（浮层交互）

**触发入口**：
1. 点击子任务 → 弹出浮层
2. 或在子任务上**右滑**露出"编辑"按钮

**浮层内容**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| **父任务** | 只读文本 | 显示父任务标题 | 自动关联 |
| **子任务标题** | 文本输入框 | 必填，1-50 字符 | 当前子任务标题 |
| **重复设置** | 可选开关 + 选择器 | 见"重复设置规则" | 当前重复设置 |
| **底部按钮** | 操作区 | "取消" / "保存" / "删除"（红色） | - |

**交互细节**：
- **不可修改归属日期/象限**：子任务继承父任务，不可单独修改
- **删除逻辑**：点击"删除" → 二次确认 → 删除子任务 → 触发父任务状态重算（见 2.2）

---

#### 快捷新增（优化操作）

**长按"+"按钮** → 弹出快捷菜单（底部弹出式菜单）：

**菜单选项**：

| 选项 | 说明 | 操作 |
|------|------|------|
| **新建任务（P0）** | 快速创建 P0 任务 | 点击后弹出新增任务浮层，象限默认选中 P0 |
| **新建任务（P1）** | 快速创建 P1 任务 | 点击后弹出新增任务浮层，象限默认选中 P1 |
| **新建任务（P2）** | 快速创建 P2 任务 | 点击后弹出新增任务浮层，象限默认选中 P2 |
| **新建任务（P3）** | 快速创建 P3 任务 | 点击后弹出新增任务浮层，象限默认选中 P3 |
| **快速添加子任务** | 为某个父任务快速添加子任务 | 点击后弹出"选择父任务"列表 → 选择父任务 → 弹出新增子任务浮层 |
| 取消 | 关闭菜单 | - |

**快速添加子任务流程**：
1. 长按"+"按钮 → 点击"快速添加子任务"
2. 弹出"选择父任务"列表（仅显示当前选中日期的任务）
   - 列表字段：任务标题、象限标识、子任务数（如"3/20"）
   - 排序：按象限 P0→P3，同象限内按创建时间升序
   - 空态：若当前日期无任务 → toast 提示"今天还没有任务，请先创建任务"
3. 点击某个父任务 → 弹出新增子任务浮层（自动关联该父任务）
4. 输入子任务标题 → 保存 → 子任务出现在父任务下方

**交互细节**：
- **长按时长**：长按 0.5 秒触发
- **菜单展示**：从底部弹出（类似系统分享菜单）
- **颜色标识**：每个象限选项前显示对应颜色圆点（P0 红色、P1 橙色等）

### 3.1.5 删除

- **删除任务/子任务**：进入 Deleted（逻辑删除）状态，默认不展示
- **回收站**：本版本不新增回收站功能
- **多端一致性**：删除必须有 `deletedAt` 字段，避免并发冲突
- **前端快捷删除**（已实现）：任务卡片左滑露出删除按钮，点击立即移除（见 3.1.3）

### 3.1.5.1 当日全完成庆祝动效（已实现）

**触发条件**：
- 仅当**今天**的所有任务首次全部完成时触发
- 每天只触发一次（状态持久化）
- 若当天已触发过，后续新增任务并完成不再触发
- 非当天（历史/未来日期）不触发

**表现形式**：
- 弹出一个轻量、居中的**礼花粒子动画**（120 个彩色粒子）
- 动画使用 CustomPainter 实现，不使用图片素材
- 显示随机励志文案（从 JSON 配置文件中随机抽取）
- 整体风格高级、克制、不浮夸
- 动画展示后自动消失（无需用户点击）

**技术实现**：
- 粒子动画组件：`confetti_animation.dart`
- 庆祝弹窗组件：`celebration_overlay.dart`
- 状态管理服务：`celebration_service.dart`（使用 shared_preferences 持久化）
- 文案配置（多语言支持）：
  - `assets/celebrations.json`（默认英文，15 条励志文案）
  - `assets/celebrations_zh.json`（中文励志文案）
  - `assets/celebrations_ja.json`（日文励志文案）
- 标题 "All Done!" 多语言：
  - 英文：All Done!
  - 中文：全部完成！
  - 日文：全部完了！

### 3.1.6 打卡入口

- **按钮位置**：页面提供"打卡/完成今日计划"按钮（位置由 UI 决定，建议顶部或底部固定）
- **打卡规则**：点击才算打卡（与完成任务解耦，见 2.1/2.9）
- **幂等性**：当日重复打卡无副作用（见状态机 5.4）

### 3.1.7 返回今天（快捷导航）

**触发条件**：
- **仅当日历选中的日期 ≠ 今天时显示**
- 按钮位置：日历右侧或顶部右上角（建议：年/月/日显示区域右侧）

**按钮样式**：
- 文案："今天" / "返回今天"
- 样式：小按钮（不影响日历主体）
- 建议图标：📅 或 ⏺（圆点图标）

**点击行为**：
1. 点击"返回今天"按钮
2. 日历自动切换到今天（高亮显示）
3. 任务列表自动刷新为今天的四象限任务
4. 按钮自动隐藏（因为当前选中 = 今天）

**交互细节**：
- **显示逻辑**：监听日历选中日期变化，当 `selectedDate ≠ today` 时显示按钮
- **隐藏逻辑**：当 `selectedDate === today` 时隐藏按钮
- **动画**：按钮显示/隐藏时增加淡入淡出动画（300ms）

**示例**：
- 用户在日历上选中"01-28（明天）" → 顶部右侧出现"返回今天"按钮
- 用户点击"返回今天" → 日历自动选中"01-27（今天）" → 按钮自动隐藏

### 3.1.8 励志名言卡片（已实现）

**位置**：四象限下方

**功能**：
- 显示励志短句，激励用户完成任务
- 每 30 分钟自动刷新为新的名言
- 应用启动时随机选择一条

**技术实现**：
- 名言数据：`assets/quotes.json`（100 条英文励志短句，无作者）
- 服务：`quote_service.dart`（单例模式，使用 Timer.periodic 定时刷新）
- 组件：`quote_card.dart`（樱花飘落动画卡片）

**视觉样式**（已升级）：
- **卡片高度**：110px（紧凑设计，不遮挡底部导航）
- **背景**：多层粉色渐变（P1 15% → primaryLight 25% → P0 10%）
- **圆角**：20px，带柔和阴影
- **樱花动画**：
  - 12 个樱花花瓣持续飘落
  - 花瓣颜色：粉白色渐变（P1 → 白色 → primaryLight）
  - 飘落速度、摇摆幅度、旋转角度各异
  - 使用 `CustomPainter` 绘制
  - 8 秒循环动画
- **装饰光晕**：
  - 多个半透明渐变圆形光晕
  - 使用 `RadialGradient` 实现
  - 营造梦幻柔和效果
- **文字动画**：
  - 呼吸效果（1.0 ↔ 1.02 缩放）
  - 3 秒循环
  - 字体 13px，最多 2 行
  - 黑色半透明（80% 透明度）
- **整体风格**：浪漫、少女、樱花主题

### 3.1.9 空态与异常（已国际化）

| 场景 | 英文文案 | 中文文案 | 引导操作 |
|------|------|------|----------|
| 所选日期无任务 | No tasks yet | 暂无任务 | 高亮底部"+"按钮 |
| 某象限无任务 | No tasks yet | 暂无任务 | 点击空白区域可快速创建 |
| 网络错误 | Network error, please try again | 网络错误，请重试 | 提供"Retry"按钮 |
| 新增任务失败 | Save failed, please retry | 保存失败，请重试 | 保留用户输入内容，提供"Retry"按钮 |

### 3.1.10 UI 语言（已实现）

**全局多语言支持**（英文/中文/日文）：

| 元素 | 英文 | 中文 | 日文 |
|------|------|------|------|
| 底部导航 | Plan / View / Focus / Me | 计划 / 视图 / 专注 / 我的 | 計画 / ビュー / 集中 / マイ |
| 返回今天按钮 | Back to Today | 返回今天 | 今日に戻る |
| 快速创建菜单 | Quick Add | 快速添加 | クイック追加 |
| 象限标题 | DO FIRST / SCHEDULE / DELEGATE / ELIMINATE | 立即执行 / 计划安排 / 尽快处理 / 延后或放弃 | 最優先 / スケジュール / 委任 / 削減 |
| 任务浮层 | New Task / Save Task | 新任务 / 保存任务 | 新規タスク / タスク保存 |
| 重复设置 | Daily / Weekly / Monthly | 每天 / 每周 / 每月 | 毎日 / 毎週 / 毎月 |
| 空态提示 | No tasks yet | 暂无任务 | タスクなし |
| 专注页面 | Pomodoro / Start Focus / End | 番茄钟 / 开始专注 / 结束 | ポモドーロ / 集中開始 / 終了 |
| 统计页面 | Week / Month / Done / Todo | 周 / 月 / 已完成 / 待办 | 週 / 月 / 完了 / 未完了 |

**四象限描述**（艾森豪威尔矩阵）：
| 象限 | 英文 | 中文 | 日文 |
|------|------|------|------|
| P0 | Urgent and important - DO IT NOW | 紧急且重要 - 现在就做 | 緊急かつ重要 - 今すぐやる |
| P1 | Important but not urgent - Schedule it | 重要不紧急 - 安排时间做 | 重要だが緊急でない - 予定を立てる |
| P2 | Urgent but not important - Delegate it | 紧急不重要 - 尽快处理 | 緊急だが重要でない - 委任する |
| P3 | Neither urgent nor important - Eliminate it | 不重要不紧急 - 可以不做 | 緊急でも重要でもない - 削除する |

---

## 3.2 视图模块

### 3.2.1 目标

视图 = 执行监控：
- 看当下要做什么
- 看哪里逾期了
- 看已经完成了什么

### 3.2.2 页面结构（已实现 + 国际化）

| 区域 | 内容 | 说明 |
|------|------|------|
| 顶部左侧 | 象限下拉筛选（All/P0/P1/P2/P3） | 支持按象限过滤任务 |
| 顶部右侧 | 时间维度切换按钮组：[周 \| 月] | Week / Month，紧凑样式 |
| 中间 | 完成率曲线图（COMPLETION RATE） | 显示 Done/Todo 比例，周视图按天(MON-SUN)，月视图按周(W1-W4) |
| 下方 | 任务列表（ONGOING TASKS） | 显示 COMPLETED / UNFINISHED 分组 |

**国际化支持**：
- 所有文案均通过 `tr()` 函数获取
- 监听语言变化自动刷新 UI
- 星期名称（MON-SUN）、周标识（W1-W4）均已国际化

### 3.2.3 默认态与记忆

见 2.6（首次默认"周 + 待执行"，且记忆用户上次选择）。

### 3.2.4 待执行 Tab 规则

#### 纳入条件
- 未完成 **且** 归属日期 ≥ today

#### 分组标题
- **周视图**：使用相对标题（今天/明天/后天…）
- **月视图**：使用绝对日期标题（如 01-27）

#### 排序
1. 先按归属日期升序
2. 同日内按优先级：P0 → P3
3. 同优先级内按创建时间升序

#### 空态
- 文案："暂无待执行任务，真棒！"

### 3.2.5 逾期 Tab 规则

#### 纳入条件
- 未完成 **且** 归属日期 < today

#### 显示
- 每条必须显示「已逾期 N 天」（N 计算见 2.5）

#### 排序（明确产品意图"越逾期越靠前"）
1. 按归属日期升序（日期越早越靠前）
2. 同日内按优先级：P0 → P3
3. 同优先级内按创建时间升序

#### 空态
- 文案："太棒了！没有逾期任务"

### 3.2.6 已完成 Tab 规则

#### 纳入条件
- 已完成

#### 排序
1. 按完成时间降序（最近完成的在前）
2. 同日内按优先级：P0 → P3
3. 同优先级内按创建时间降序

#### 空态
- 文案："暂无已完成任务\n完成任务后会在这里看到哦~"

### 3.2.7 范围边界（周/月）

- **仅展示当前周/当前月范围内任务**
- **范围外任务不在该视图出现**
- **逾期任务展示规则**：
  - 逾期任务的"归属日期"如果**落在当前周/月范围内**，则显示在逾期 Tab
  - 逾期任务的"归属日期"如果**不在当前周/月范围内**，则不显示
  - **不做"把所有历史逾期都塞进本周/本月"的合并逻辑**

#### 示例（当前周：2026-01-26 ~ 2026-02-01）
- 任务 A：归属日期 2026-01-25（逾期），**不显示**（不在本周范围）
- 任务 B：归属日期 2026-01-26（逾期），**显示**在逾期 Tab
- 任务 C：归属日期 2026-01-27（待执行），**显示**在待执行 Tab

---

## 3.3 专注模块

### 3.3.1 产品定位

专注 = 一个纯倒计时的番茄钟，用来开始，不用来复盘。

### 3.3.2 页面结构（已实现 + 国际化）

| 区域 | 内容 | 说明 |
|------|------|------|
| 顶部标题 | "🍅 Pomodoro" | 番茄钟图标 + 标题（已国际化） |
| 顶部右侧 | 历史记录按钮（P0红色） | 点击弹出专注历史浮层 |
| 中间 | 倒计时圆盘 + 呼吸光晕动画 | 显示剩余时间 mm:ss |
| 圆盘下方 | 状态文字 | "Focus in progress..." / "Ready to focus" |
| 下方按钮区 | Set Time / Start Focus / Pause / End | 根据状态显示不同按钮 |
| 底部 | Auto-start next round 开关 | 开启后自动开始下一轮番茄钟 |

**国际化支持**：
- 所有按钮、提示、浮层文案均通过 `tr()` 函数获取
- 监听语言变化自动刷新 UI
- 专注历史浮层（Total Focus Time / Longest Session / Focus Sessions）已国际化

### 3.3.3 时长选择

- **默认**：25 分钟
- **固定可选时长**：15 / 25 / 30 / 45 / 60（分钟）
- **不支持自定义分钟**，不支持正计时
- **参数锁定**：仅在未开始（Idle）可选择时长；**计时中不可修改**（见 2.7）

### 3.3.4 类型选择

- **固定内置类型**（示例）：工作/学习/阅读/编程/运动/冥想/其它
- **只能选一个**
- **不支持新增/编辑**
- **仅用于统计总时长**（不做记录列表）

### 3.3.5 结束与计入规则

严格按 2.7 执行（自然结束计入；手动结束按 50% 阈值计入/不计入）。

### 3.3.6 异常场景

| 场景 | 处理 |
|------|------|
| 计时中后台（锁屏/切到其他 App） | 进入"已中断"状态（见状态机 5.5）；**后台继续计时**（倒计时不暂停） |
| 计时中来电 | 同上 |
| 恢复前台 | 从"已中断"恢复到"计时中" |
| App 被杀进程（强制关闭） | 会话丢失，**不计入总时长** |

---

## 3.4 我的模块

### 3.4.1 页面结构（已实现）

| 区域 | 内容 | 说明 |
|------|------|------|
| 顶部 | 用户头像（80x80，可点击更换） | 圆形头像，带粉色渐变边框和阴影 |
| 头像下方 | 用户昵称（可点击编辑） | 行内编辑模式，点击笔形图标直接编辑 |
| 昵称下方 | 坚持天数统计 | "Planning Streak **N** Days"，N 使用 P0 玫红色 |
| Account 区 | 账号管理分组 | 修改密码、退出登录 |
| Settings 区 | 系统设置分组 | 音效开关、设备管理、语言设置 |

### 3.4.2 用户头像（已实现）

#### 展示
- 圆形头像（80x80px）
- 粉色渐变边框（P0→P1）
- 右下角编辑图标标识

#### 编辑
- 点击头像弹出选择浮层
- 提供 **20 个预设 SVG 头像**供选择（avatar21-avatar40）
- 浮层高度：屏幕 50%，支持滚动
- 头像以 5 列网格布局展示
- 选中头像有高亮边框和阴影
- 默认头像：avatar26.svg
- 选择后自动保存（持久化）

### 3.4.2.1 用户昵称（已实现）

#### 展示
- 昵称显示在头像下方
- 右侧显示笔形编辑图标（✎）
- 默认昵称："JohnGao"

#### 编辑（行内编辑模式）
- 点击昵称或编辑图标 → 直接在原位显示输入框
- 输入框自动获取焦点
- 失焦或按回车自动保存
- 空值保护：若输入为空则恢复原昵称
- 字符限制：1-20 字符
- 昵称持久化：使用 `SharedPreferences` 保存
- 昵称同步：修改后自动更新做计划页面左上角显示

### 3.4.3 坚持天数（已实现）

- 显示格式："Planning Streak **N** Days"
- 数字 N 使用 P0 玫红色突出显示
- 连续打卡天数统计

### 3.4.4 Account 分组（已实现）

#### 修改密码
- 点击弹出修改密码浮层
- 输入字段：当前密码、新密码、确认新密码
- 每个输入框右侧有密码可见/隐藏切换按钮
- 验证规则：
  - 所有字段必填
  - 新密码至少 6 位
  - 两次输入必须一致
- 提交按钮：P0→P1 渐变色胶囊按钮 + 箭头图标（与 New Task 一致）
- 成功后显示 SnackBar 提示

#### 退出登录
- 点击弹出确认对话框
- 对话框样式：圆角卡片 + 图标 + 标题 + 描述 + 按钮组
- 确认后跳转到登录页

### 3.4.5 Settings 分组（已实现）

#### 音效开关
- Switch 开关控制全局音效
- 关闭后所有音效静音
- 状态持久化

**音效列表**（`audio_service.dart` 管理）：
| 音效 | 文件 | 触发场景 |
|------|------|----------|
| 按钮音 | `toy-button-105724.mp3` | 所有按钮点击（保存、取消、确认等） |
| 翻页音 | `turn-a-page-336933.mp3` | 日历左右翻页切换周 |
| 点击音 | `computer-mouse-click-352734.mp3` | 底部导航切换、日期选择 |
| 完成音 | `game-over-417465.mp3` | 番茄钟倒计时结束 |
| 庆祝音 | `woman-cute-silly-ya-3-185320.mp3` | 当日任务全部完成 |
| 猫咪喵叫 | `cat-meow-401729.mp3` | 点击做计划页面的可拖动猫咪 |

#### 设备管理
- 点击弹出设备列表浮层
- 显示已登录设备列表（模拟数据）
- 每个设备显示：设备名称、IP 地址、最近登录时间
- 当前设备标记 "Current" 标签，不可操作
- 非当前设备显示 "Logout" 按钮
- 顶部显示设备数量：如 "3/10 devices logged in"
- 设备数量上限：10 台

#### 语言设置（已实现）
- 点击弹出语言选择浮层
- 显示三个语言选项：
  - English（默认）
  - 中文
  - 日本語
- 当前语言带选中标记
- 选择后立即切换，无需重启
- 语言持久化：使用 `SharedPreferences` 保存
- 影响范围：所有页面、浮层、对话框、底部导航栏文案

### 3.4.6 登录机制（已实现 + 国际化）

#### 登录方式
- **邮箱 + 密码登录**（前端模拟，不涉及后端）
- 测试账号：`gxl@gmail.com` / `123456`
- 支持注册新账号

#### 登录页面
- APP 启动时始终进入登录页
- 标题根据语言显示：Maiden Plan / 少女计划 / 少女の計画
- 输入邮箱和密码
- 登录失败时输入框显示红色高亮提示
- 底部提供注册和找回密码入口
- 登录按钮：P0→P1 渐变胶囊按钮 + 箭头图标

#### 注册页面
- 标题颜色与登录页一致（P0 玫红色）
- 输入邮箱、密码、确认密码
- 注册按钮样式与登录按钮一致：P0→P1 渐变胶囊 + 箭头图标
- 验证通过后自动登录并跳转主页

#### 国际化支持
- 登录/注册页面所有文案均通过 `tr()` 函数获取
- 找回密码弹窗已国际化

### 3.4.7 做计划页面头部（已实现）

#### 左上角
- 圆形头像（48x48px）+ 粉色边框 + 阴影
- 昵称（默认 "JohnGao"，与 Me 页面同步）
- 副标题："已坚持做计划 **N** 天"
  - 多语言支持：
    - 英文：Planning Streak **N** Days
    - 中文：已坚持做计划 **N** 天
    - 日文：計画を続けて **N** 日
  - 数字 N 使用 P0 玫红色高亮显示
  - 监听语言变化自动刷新

#### 右上角
- 设置按钮（圆形，粉色边框）
- 点击跳转到 Me 页面

---

## 4. 异常与边界（落地必须覆盖）

### 4.1 幂等与并发（多端一致性）

| 操作 | 冲突解决方案 |
|------|--------------|
| 勾选完成/反完成 | 同一任务的并发写入，以"最后写入时间戳/版本号"解决冲突（具体方案由研发选型） |
| 删除/踢下线 | 幂等（重复请求不应报错或产生二次副作用） |
| 支付回调 | 订单幂等（同一订单号仅生效一次） |
| 打卡 | 幂等（当日重复打卡无副作用） |

### 4.2 时间与时区

- 逾期计算、7 天游标等一律以**用户本地时区**自然日为准
- 每天 00:00 结算昨日打卡与连续未打卡计数
- **服务端存储**：建议使用 UTC 时间戳，客户端展示时转换为本地时区

### 4.3 专注中断（后台/来电/锁屏）

- 专注会话状态允许进入 `Interrupted` 并可 `Resume`
- **后台计时策略**：**后台继续计时**（倒计时不暂停）
- **App 被杀进程**：会话丢失，不计入总时长

### 4.4 网络异常与限流

| 场景 | 处理 |
|------|------|
| 网络断开（离线） | toast 提示"网络连接失败"；**本地缓存任务变更**，网络恢复后同步 |
| 接口超时（> 10s） | toast 提示"请求超时，请重试"；提供"重试"按钮 |
| 服务端限流（429） | toast 提示"操作过于频繁，请稍后再试"；**客户端加防抖（1s）** |
| 服务端错误（500） | toast 提示"服务异常，请稍后重试" |

### 4.5 数据量限制

| 项目 | 上限 | 超出提示 |
|------|------|----------|
| 单日任务数 | 50 条（不含重复任务副本） | "今日任务数已达上限（50 条），请完成后再创建" |
| 单任务子任务数 | 20 条 | "子任务数已达上限（20 条）" |
| 重复任务副本数 | 单个重复任务最多生成 365 个副本 | "重复任务副本数已达上限（365 条）" |
| 设备数 | 10 台 | "设备数量已达上限，请先退出其他设备" |
| 昵称修改频率 | 7 天内 2 次 | "昵称修改过于频繁，请 X 天后再试" |

### 4.6 重复任务异常处理

| 场景 | 处理 |
|------|------|
| 重复任务生成失败（服务端异常） | toast 提示"重复任务生成失败，请稍后重试"；任务本身创建成功，但不生成副本 |
| 副本数量达到上限（365 条） | toast 提示"重复任务副本数已达上限（365 条）"；停止生成新副本 |
| 删除重复任务源 | 弹窗二次确认："是否删除所有重复任务（含未来副本）？"<br>• 确认 → 删除源任务 + 所有未完成副本<br>• 取消 → 仅删除当前任务（不影响副本） |
| 修改重复规则 | 仅影响未来副本（已生成的副本保持原样）；toast 提示"修改仅对未来副本生效" |
| 重复任务副本生成冲突（同一天已有同名任务） | 自动在标题后添加序号（如"早起跑步 (2)"） |

---

## 5. 附录

## 5.1 术语表

| 术语 | 定义 |
|------|------|
| **归属日期** | 任务所属的日期（天视角），用于日历展示与视图聚合 |
| **完成态过滤** | 控制已完成任务是否展示的开关（开=显示删除线；关=隐藏） |
| **打卡** | 点击"打卡/完成今日计划"按钮的行为，**不等于**完成任务 |
| **坚持天数** | 连续打卡的自然日天数 |
| **逾期** | 任务归属日期 < 今天，且未完成 |
| **计入** | 专注会话完成后，累加到"总专注时间" |
| **免费期** | 非会员免费使用 1 个月，从首次微信授权登录起算 |
| **已注销（Deactivated）** | 连续 7 天未打卡后账号被逻辑禁用，无法再登录 |
| **重复任务** | 设置了重复规则的任务，系统自动在未来日期生成任务副本（最多 365 天） |
| **重复任务副本** | 由重复任务源自动生成的任务实例，可独立完成/编辑/删除 |

---

## 5.2 关键状态机

### 5.2.1 任务状态机（父任务）

**说明**：父任务存在子任务时，完成状态由子任务派生（见 2.2）。

```mermaid
stateDiagram-v2
  state "未完成" as Incomplete
  state "已完成" as Complete
  state "已删除" as Deleted

  [*] --> Incomplete: 创建任务

  Incomplete --> Complete: 勾选完成（仅当无子任务或所有子任务已完成）
  Complete --> Incomplete: 反勾选

  Incomplete --> Incomplete: 编辑（标题/优先级/归属日期）
  Complete --> Complete: 编辑（标题/优先级/归属日期）

  Incomplete --> Incomplete: 添加子任务
  Complete --> Incomplete: 添加子任务（自动变未完成）

  Incomplete --> Complete: 所有子任务完成（自动）
  Complete --> Incomplete: 任一子任务反完成（自动）

  Incomplete --> Deleted: 删除任务
  Complete --> Deleted: 删除任务
  Deleted --> [*]
```

### 5.2.2 子任务状态机

**说明**：子任务完成/反完成会自动触发父任务状态联动（见 2.2）。

```mermaid
stateDiagram-v2
  state "未完成" as SubIncomplete
  state "已完成" as SubComplete
  state "已删除" as SubDeleted

  [*] --> SubIncomplete: 创建子任务（挂载在父任务下）

  SubIncomplete --> SubComplete: 勾选完成（触发父任务重算）
  SubComplete --> SubIncomplete: 反勾选（触发父任务重算）

  SubIncomplete --> SubIncomplete: 编辑标题
  SubComplete --> SubComplete: 编辑标题

  SubIncomplete --> SubDeleted: 删除子任务（触发父任务重算）
  SubComplete --> SubDeleted: 删除子任务（触发父任务重算）
  SubDeleted --> [*]
```

### 5.2.3 账号状态机（包含打卡与注销）

**说明**：账号状态由连续打卡天数驱动（见 2.9）；每日打卡状态见下方流程图。

```mermaid
stateDiagram-v2
  state "正常（N天坚持）" as ActiveN {
    [*] --> Day0: 首次登录/注册
    Day0 --> Day1: 次日打卡（N=1）
    Day1 --> Day2: 次日打卡（N=2）
    Day2 --> DayN: 持续打卡（N递增）
    DayN --> Day0: 某日未打卡（N清零）
    Day1 --> Day0: 某日未打卡（N清零）
    Day2 --> Day0: 某日未打卡（N清零）
  }

  state "已注销" as Deactivated

  [*] --> ActiveN: 首次微信授权登录

  ActiveN --> Deactivated: 连续7天未打卡（定时任务触发）
  Deactivated --> [*]: 物理删除数据
```

### 5.2.4 每日打卡状态（单日视角）

**说明**：完成任务不等于打卡；仅点击"打卡/完成今日计划"按钮才算打卡（见 2.1）。

```mermaid
stateDiagram-v2
  state "未打卡" as NotCheckedIn
  state "已打卡" as CheckedIn

  [*] --> NotCheckedIn: 当日开始（00:00）

  NotCheckedIn --> CheckedIn: 点击打卡按钮
  CheckedIn --> CheckedIn: 再次点击打卡按钮（幂等/无操作）

  NotCheckedIn --> NotCheckedIn: 完成任务（不触发打卡）
  CheckedIn --> CheckedIn: 完成任务

  NotCheckedIn --> [*]: 当日结束（23:59:59/进入次日00:00）
  CheckedIn --> [*]: 当日结束（23:59:59/进入次日00:00）

  note right of CheckedIn
    打卡成功会：
    1. 连续打卡天数+1
    2. 连续未打卡计数清零
    3. 停止当日未打卡推送
  end note
```

### 5.2.5 专注会话状态机

**说明**：计入规则见 2.7（自然结束计入；手动结束按 50% 阈值判定）。

```mermaid
stateDiagram-v2
  state "空闲" as Idle
  state "计时中" as Running
  state "已中断" as Interrupted

  [*] --> Idle

  Idle --> Running: 点击开始（选择时长/类型后锁定）
  Running --> Idle: 自然结束（计入总时长）
  Running --> Idle: 手动结束且>=50%（计入总时长）
  Running --> Idle: 手动结束且<50%（不计入）

  Running --> Interrupted: 后台/来电/锁屏
  Interrupted --> Running: 恢复前台/继续
  Interrupted --> Idle: 手动结束且>=50%（计入总时长）
  Interrupted --> Idle: 手动结束且<50%（不计入）

  note right of Running
    计时中不可修改时长/类型
  end note
```

### 5.2.6 会员权益状态机

**说明**：免费期从首次登录起算 1 个月；续费叠加到期时间；支付幂等（见 3.4.2）。

```mermaid
stateDiagram-v2
  state "未激活" as NotEntitled
  state "免费期有效" as FreeTrial
  state "会员有效" as MemberActive
  state "已到期" as Expired

  [*] --> NotEntitled: 账号创建

  NotEntitled --> FreeTrial: 首次微信授权登录（记录trialStartAt）
  NotEntitled --> MemberActive: 直接购买会员（跳过免费期/记录expireAt）

  FreeTrial --> MemberActive: 购买会员（记录expireAt）
  FreeTrial --> Expired: 免费期到期（now>=trialStartAt+1month）

  MemberActive --> MemberActive: 续费成功（延长expireAt）
  MemberActive --> Expired: 会员到期（now>=expireAt）
  MemberActive --> Expired: 退款/拒付

  Expired --> MemberActive: 重新购买（记录新expireAt）
  Expired --> Expired: 到期后不购买（保持到期状态）

  note right of FreeTrial
    免费期内购买会员：
    免费期立即失效，
    按购买时点起算会员有效期
  end note
```

### 5.2.7 设备登录会话状态机

**说明**：单个设备的登录会话状态；多设备管理见 2.8。

```mermaid
stateDiagram-v2
  state "未登录" as LoggedOut
  state "已登录" as LoggedIn

  [*] --> LoggedOut

  LoggedOut --> LoggedIn: 微信授权成功（生成设备token）
  LoggedIn --> LoggedOut: 用户主动退出当前设备
  LoggedIn --> LoggedOut: 被其他设备踢下线
  LoggedIn --> LoggedOut: token过期/失效

  LoggedOut --> [*]
  LoggedIn --> [*]: 账号注销（所有设备强制下线）

  note right of LoggedIn
    登录状态维护：
    - 设备标识
    - 登录token
    - 最近活跃时间
  end note
```

---

## 📝 变更记录

| 版本 | 日期 | 变更内容 | 修订人 |
|------|------|----------|--------|
| v1.0 | 2026-01-27 | 初稿（基于原 PDF + 歧义修复） | - |
| v2.0 | 2026-01-27 | 补齐功能边界/异常场景/字段约束；修复歧义；优化排版 | AI 产品助理 |
| v2.1 | 2026-01-31 | 同步前端实现：更新"我的模块"为已实现功能（头像选择、音效设置、设备管理、修改密码）；登录改为邮箱密码方式；全局UI英文化；更新模块实现状态 | AI 开发助理 |
| v2.2 | 2026-01-31 | 多语言支持：新增英/中/日三语言系统；Me→Settings增加语言切换；所有页面、浮层、对话框完成国际化；更新四象限标题/描述为艾森豪威尔矩阵风格；头像扩展至20个；昵称改为行内编辑；做计划页面头部副标题改为"已坚持做计划N天"并支持多语言；励志名言卡片重新设计为樱花飘落动画效果；庆祝弹窗支持多语言励志文案；可拖动猫咪点击触发喵叫音效 | AI 开发助理 |

---

## 📋 前端实现状态

| 模块 | 状态 | 备注 |
|------|------|------|
| 登录/注册 | ✅ 已实现 | 邮箱密码登录，前端模拟，全部国际化 |
| 做计划 | ✅ 已实现 | 四象限、日历、任务管理、子任务、重复任务，全部国际化 |
| 视图 | ✅ 已实现 | 周/月视图、待执行/逾期/已完成 Tab，全部国际化 |
| 专注 | ✅ 已实现 | 番茄钟倒计时、专注历史统计，全部国际化 |
| 我的 | ✅ 已实现 | 头像（20个）、昵称（行内编辑）、音效、语言设置、设备管理、修改密码、退出登录，全部国际化 |
| 音效系统 | ✅ 已实现 | 6种音效：按钮音、翻页音、鼠标点击音、完成音、庆祝音、猫咪喵叫音 |
| 庆祝动画 | ✅ 已实现 | 粒子飘落 + 多语言励志文案 |
| 可拖动猫咪 | ✅ 已实现 | 趴在底部按钮上，可拖动，点击触发喵叫音效并摆尾 |
| 多语言系统 | ✅ 已实现 | 支持英文/中文/日文，实时切换，持久化保存 |
| 励志名言卡片 | ✅ 已实现 | 樱花飘落动画 + 呼吸文字效果 |

---

**文档结束**
